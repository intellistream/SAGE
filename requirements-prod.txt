# SAGE 生产环境安装文件
# 使用方法: pip install -r requirements-prod.txt
# 注意：这个文件会将SAGE安装到site-packages，而不是开发模式

# 方法1: 使用本地wheels（推荐）
# 先运行: make build
# 然后运行: pip install ./build/wheels/sage*.whl

# 方法2: 使用此requirements文件进行有序安装
# 注意：需要按依赖顺序安装，避免循环依赖

# 基础依赖（必须先安装）
torch==2.7.1
torchvision==0.22.1
transformers>=4.40.0
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
numpy>=1.21.0
pyyaml>=6.0.0

# SAGE 核心组件（按依赖顺序）
./build/wheels/sage_kernel-*-py3-none-any.whl
./build/wheels/sage_middleware-*-py3-none-any.whl  
./build/wheels/sage_userspace-*-py3-none-any.whl
./build/wheels/sage-*-py3-none-any.whl

# 可选：CLI工具
# ./build/wheels/sage_cli-*-py3-none-any.whl
# ./build/wheels/sage_dev_toolkit-*-py3-none-any.whl
