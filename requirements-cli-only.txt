# SAGE CLI工具安装（轻量级命令行工具）
# 基于核心包安装

# 需要先安装: pip install -r requirements-core.txt

# CLI包
-e ./packages/sage-tools/sage-cli

# 用户界面工具（轻量级）
questionary>=1.10.0
prompt_toolkit>=3.0.50

# 说明：CLI包设计为轻量级，避免重型AI依赖
