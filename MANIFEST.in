# MANIFEST.in - 告诉setuptools包含哪些非Python文件

# 注意：C++扩展文件现在通过CMake独立管理，不再通过setup.py包含
# sage_ext/sage_queue/ 下的C++文件由独立构建系统管理

# 包含配置文件
recursive-include config *.yaml *.yml
recursive-include deployment/config *.yaml *.yml

# 包含脚本文件
recursive-include deployment/scripts *.sh
recursive-include deployment/templates *

# 包含文档
include README.md
include LICENSE
include *.md

# 排除测试文件（注释掉以避免安装测试文件）
# recursive-include tests *
# recursive-include sage_tests *

# 排除不需要的文件
global-exclude *.pyc
global-exclude *.pyo
global-exclude *~
global-exclude __pycache__
global-exclude .git*
global-exclude .DS_Store

# 显式排除测试目录
recursive-exclude tests *
recursive-exclude */tests *
recursive-exclude */test *
prune tests
prune */tests
prune */test
